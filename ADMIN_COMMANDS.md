# 笨猫 · 主人专属命令速查

所有命令只对 `config.json` 里 `qq.owner_qq`（= `catty_owner_qq`）匹配的 QQ 生效。
其他人发同样的话**完全无效**（要么被直接 ignore，要么走普通 AI 回复）。

私聊和群聊默认都能用；带 `<qq>` 参数的命令一律是**针对那个 QQ 操作**，不会动你自己。

---

## 1. 签到 / 积分 / 好感度管理

显式斜杠命令，主匹配器 `aff_admin_matcher`（priority=44）。

| 命令 | 作用 |
|---|---|
| `/aff_show <qq>` | 查那个用户的积分、等级、签到/消费记录 |
| `/aff_reset_signin <qq>` | 擦掉他「今日已签到」标记，让他能再签一次 |
| `/aff_set_points <qq> <n>` | 把他积分**设成** `n`（n ≥ 0） |
| `/aff_add_points <qq> <n>` | 给他**加/减** `n`（可负，最低 clamp 到 0） |
| `/aff_set_exp <qq> <n>` | 直接设好感度经验，等级自动重算 |
| `/aff_reset <qq>` | 整本账撕掉，从零开始（积分/经验/签到/消费全清） |

主人自己 (`is_owner`) 的积分永远是 `OWNER_INFINITY_POINTS`、等级永远 `LEVEL_CAP`，
对主人 QQ 用这些命令大多是 no-op；`aff_show` 会直接提示「是主人本人」。

主人自己**已经免每日一次限制**，可以无限次发「签到」连刷卡。

---

## 2. 用户画像 (vibe profile) 管理

显式斜杠命令，主匹配器 `vibe_command_matcher`（priority=43）。

| 命令 | 作用 |
|---|---|
| `/vibe_show <qq>` | 查那个用户的笨猫画像：主调 vibe / 常聊 topic / 累计消息 / 置信度 |
| `/vibe_show` | 不带 QQ 就查自己的 |
| `/vibe_reset <qq>` | 清掉那个用户的画像（必须带 QQ，防误清自己） |

---

## 3. 表情收藏

主匹配器 `emoji_save_matcher`（priority=41）。
图源优先级：本条消息附图 → 引用消息附图 → 最近 5min 群图。

支持的前缀（任意一个 + 可选 tag）：

```
收藏 / 收藏表情 / 收藏这个表情 / 收藏这个 / 收藏图
存表情 / 存这个表情 / 存这个 / 存图
加表情 / 加表情库 / 入库
/saveemoji / /emoji
```

tag 用空格、逗号、`#`、分号都行；留空时笨猫会看图自动取 tag。

例：
- `收藏` → 用最近图源 + AI 自动 tag
- `收藏 开心 喵呜` → tag = ["开心", "喵呜"]
- `收藏#开心#无奈` → tag = ["开心", "无奈"]

---

## 4. 上下文 / 会话 / 缓存管理（主聊天流内短路）

这些不是独立 matcher，而是在主 `chat_matcher` 内部判定：
**必须发言人是主人 + 文本命中关键词**，否则当普通消息走 AI 回复。

### 4.1 清空当前对话上下文

```
reset / /reset / clear / /clear
清空 / 清空上下文 / 重置 / 重置上下文 / 忘掉上文
```

→ 清掉**当前会话**的 history，回复「上下文清掉啦。」

### 4.2 列出所有会话（**仅私聊**）

```
ai会话列表 / ai列会话 / ai列出会话 / ai查看会话 / ai看看会话
ai会话 / ai所有会话 / ai sessions / aisessions
/sessions / /会话列表 / /会话
会话列表 / 列会话 / 查看会话 / 看看会话
```

→ 在私聊里列出所有缓存中的会话（群/私聊）。群里发这条不响应。

### 4.3 清空记忆缓存

```
clearcache / /clearcache
清空缓存 / 清除缓存 / 清理缓存
清空记忆缓存 / 清除记忆缓存
清空当前缓存 / 清空当前记忆缓存
```

→ 同时清当前会话 history + memory_store 的当前 scope 缓存。

### 4.4 查看记忆 (memory view)

**精确命中：**

```
memory / /memory / 记忆
查看记忆 / 看看记忆 / 显示记忆 / 读取记忆
查看存储 / 查看人物信息 / 查看群友信息
查看人物画像 / 查看群友画像
你记得我什么
```

**模糊命中**（长度 ≤ 20 字 + 不含画图词）：
含「查看 / 看看 / 显示 / 调出 / 读取 / 列出」**任一** 
× 含「记忆 / 存储 / 人物信息 / 群友信息 / 画像」**任一**

→ 输出 memory_store 对当前 scope 的内部 dump。
> ⚠️ 含 `D:\` 路径和群 JSON 文件名等内部状态，**仅主人可触发**，避免泄露给群友。

---

## 5. 隐式行为差异（不算命令但跟主人身份相关）

主人发消息时笨猫**额外行为**：

- **称呼**：永远叫「主人 / 笨蛋主人 / 杂鱼主人」，普通用户严禁这些称呼。
- **积分/等级**：无限积分、`LEVEL_CAP` 等级，所有消费/扣分 no-op。
- **wake-up**：主人发消息默认 fallthrough 给主 AI（不被 critic/filter 短路）。
- **placeholder 等候语**：使用主人专属池子，可以用更亲昵的词。
- **戳一戳响应**：戳笨猫触发主人专属的撒娇回复，普通用户走另一组。
- **persona hint**：注入「这是你最依赖的主人」段落，傲娇撒娇基调放最大。

---

## 6. 配置入口

主人 QQ 在 `config.json`：

```json
{
  "qq": {
    "owner_qq": "你的QQ号"
  }
}
```

留空或 `"0"` 时整个主人豁免/管理体系**全部失效**，所有用户走普通路径。

---

## 7. 速查总表

| 类别 | 命令样例 | 触发条件 |
|---|---|---|
| 签到管理 | `/aff_show 123456` | 主人 + 命令 |
| 画像管理 | `/vibe_show 123456` | 主人 + 命令 |
| 表情收藏 | `收藏 开心` | 主人 + 前缀 + 有图 |
| 清上下文 | `reset` | 主人 + 关键词 |
| 列会话 | `会话列表` | 主人 + 关键词 + 私聊 |
| 清缓存 | `clearcache` | 主人 + 关键词 |
| 看记忆 | `查看记忆` | 主人 + 关键词 |

ฅ 笨猫等主人来用哦嗷呜～ ฅ
